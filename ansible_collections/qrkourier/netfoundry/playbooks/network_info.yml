
- hosts: localhost
  collections:
  - qrkourier.netfoundry
  tasks:
  - name: find Network resources
    netfoundry_info:
      networkName: BibbidiBobbidiBoo
      credentials: dwstaging.json # relative to playbook
    register: netfoundry_info
  - name: dump test output
    debug:
      msg: '{{ netfoundry_info }}'

  #   - name: create Endpoint
  #     uri:
  #       url: "https://gateway.production.netfoundry.io/core/v2/endpoints"
  #       body_format: json
  #       method: POST
  #       headers:
  #           Content-Type: application/json
  #           Authorization: Bearer {{lookup('env', 'NETFOUNDRY_API_TOKEN')}}
  #       body:
  #           networkId: '{{ networkId }}'
  #           name: '{{ endpointName }}'

  # vars:
  #   networkId:
  #   endpointName: